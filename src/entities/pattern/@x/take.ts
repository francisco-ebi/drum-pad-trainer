/** Public surface of `entities/pattern` for `entities/take` (FSD @x
 *  cross-import notation, §13.2). The judge talks about voices and hands; it
 *  never reaches for seeds, validation or rendering helpers. */
export type { Hand, Hit, Pattern, Voice } from '../model/types'
export { VOICE_META } from '../config/voices'
